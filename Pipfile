[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
pytest = "*"
tox = "*"

[scripts]
build = "python setup.py sdist"
install = "pip install -e ."
test = "pytest"
